groups:
- name: alert-djiby
  rules:
  - alert: HighErrorRate
    expr: job_request_duration_seconds_count > 0.01
    for: 5m
    labels:
      severity: page
    annotations:
      description: 'High error rate on {{ $labels.instance }}'
      summary: 'High error rate on {{ $labels.instance }}'
